# .emacs.d

## setup

```node
# for html
$ brew install tidy-html5 # or sudo apt install tidy

# for js
$ npm i -g javascript-typescript-langserver
$ npm i -g eslint

# for css
$ npm i -g stylelint
```
